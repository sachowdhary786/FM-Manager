---
import "./style.scss";
import {CardInterface} from './card.type';

const { href, title, description }: CardInterface = Astro.props;
---

<li class=`link-card`>
	<a href={href}>
		<h2>{title}</h2>
		<p>{description}</p>
	</a>
</li>
